version: 2

sources:
  - name: raw
    database: RAW
    schema: PUBLIC
    tables:
      - name: customer
        columns:
          - name: id
            tests:
              - not_null
              - unique